# AGP 순이익 대시보드 — 에이전트 작업 규칙

이 저장소에서 갱신·수정 작업을 하는 모든 에이전트(Claude/Codex)는 아래 규칙을 따른다.

## 운영 변동비율 정본: 11.09% (2026-08-10 소유자 확정)

```
운영 변동비 = 결제액 합계 × (10/110 + 0.02)
            = 부가세 9.0909% + 기타 마케팅비 1% + 기타 물류비 1%
```

- **부가세는 10/110이다.** 매출(결제액)은 VAT 포함 금액이므로 매출세액은 결제액×10/110.
  결제액×10%로 곱하면 0.91%p 과대 차감 — 과거(≤2026-08-09) 방식이며 금지.
- **매입세액공제를 추가로 반영하지 않는다.** SKU 원가·광고비가 부가세 제외 금액으로
  기록되므로, 매출세액 전액 차감 방식에 매입세액공제가 이미 암묵 반영돼 있다.
  "실제 납부세액은 더 적으니 빼자"는 조정은 이중 반영이므로 금지.
- 12%(부가세 10%) 기준으로 되돌리지 않는다. 비율 변경은 소유자 지시로만.

## 적용 위치 (세 곳이 항상 일치해야 함)

1. **Supabase 뷰** `mart_daily_profit_gauge_source` — `operating_variable_cost`와
   `daily_net_profit_before_fixed`의 `* (10.0 / 110.0 + 0.02)`
2. **index.html** — `const operatingVariableRate = 10 / 110 + 0.02;` 및 `variableCosts` 표기
3. **화면 라벨** — "11.09%", "부가세 10/110(9.09%)" 문구

셋 중 하나만 바꾸면 프론트/DB 수치가 갈라진다. 변경 시 세 곳 동시 수정 + 아래 검증.

## 갱신(리프레시) 시 필수 순서

1. 자사몰/네이버 러너 재적재가 **끝난 뒤** 스냅샷을 뜬다. (2026-08-10 사고:
   09:31 빌드 직후 09:39 자사몰 8/3 재적재 → 배포본이 8분 차이로 스테일, 순수익 +9.2만 과대)
2. `tools/build_daily_rows.py --month YYYY-MM`으로 행 재생성.
3. 빌드 직후 임베디드 합계와 `mart_daily_profit_gauge_source` 라이브 합계를 대조(assert).
   불일치하면 배포 금지하고 재빌드.
4. `tools/test_*.py` 전체 실행 — 완결성 테스트의 기준일 하드코딩(`latest_dashboard_basis`)도
   새 기준일로 함께 갱신할 것 (8/9 갱신 때 누락돼 suite가 깨진 전례 있음).

## 기타 확정 사항

- 고정비 정본은 `dim_agp_fixed_cost_monthly`(DB)와 index.html `fixedCostsByMonth`가 동일 값.
- 네이버 수수료는 6.8%다. 배송부담은 결제일 기준으로 2026-07-01 이전
  6,200원/주문, 2026-07-01부터 6,442원/주문을 적용한다. 한 개의 영구
  고정값으로 되돌리지 않는다.
- DB 뷰 변경은 supabase MCP가 읽기 전용이므로 `~/Documents/codex`에서
  `supabase db query --linked -f <sql>` 경로를 쓴다 (`db push` 금지).
